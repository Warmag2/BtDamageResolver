# Faemiyah.BtDamageResolver.Common

Common helper methods and classes for BtDamageResolver.

This package bundles shared configuration, logging and utility helpers used
across the BtDamageResolver server and client. It is useful if you want to
build your own client against a resolver cluster.
