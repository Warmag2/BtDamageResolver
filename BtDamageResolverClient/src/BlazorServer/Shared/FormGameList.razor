@using Faemiyah.BtDamageResolver.Api.Entities.RepositoryEntities

<table class="resolver_table resolver_table_gamelist">
    <tr>
        <th>
            Game Name
        </th>
        <th>
            Players
        </th>
        <th>
            Password
        </th>
        <th>
            Last activity
        </th>
        <th>
        </th>
    </tr>
    @if (Games != null)
    {
        @foreach (var game in Games)
        {
            <FormGameEntry @key="game.GetId()" Game="@game" OnJoin="@OnGameJoin"></FormGameEntry>
        }
    }
</table>


@code {
    [Parameter]
    public List<GameEntry> Games { get; set; }

    [Parameter]
    public EventCallback<string> OnJoin { get; set; }

    private void OnGameJoin(string gameName)
    {
        OnJoin.InvokeAsync(gameName);
    }
}
