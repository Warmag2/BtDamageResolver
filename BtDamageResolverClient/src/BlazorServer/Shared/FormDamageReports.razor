@inherits BaseFaemiyahComponent
@using Faemiyah.BtDamageResolver.Api.Entities
@using Faemiyah.BtDamageResolver.Api.Enums;
@using Faemiyah.BtDamageResolver.Client.BlazorServer.Logic
@inject UserStateController _userStateController
@implements IDisposable

@if (_userStateController.DamageReportContainer.IsEmpty() || _userStateController.PlayerState == null)
{
    <h2 class="resolver_header">
        No damagereports available
    </h2>
}
else
{
    @foreach (var singleTurnDamageReports in _damageReportsToShow.Reverse())
    {
        <ContainerAccordion Enabled=@_userStateController.DamageReportContainer.Visible(singleTurnDamageReports.Key) Title=@($"Turn {singleTurnDamageReports.Key}")>
            @foreach (var damageReports in singleTurnDamageReports.Value.GroupBy(dr => $"{dr.TargetUnitId}-{dr.Phase}"))
            {
                <FormDamageReportContainer DamageReports=@damageReports.ToList()></FormDamageReportContainer>
            }
        </ContainerAccordion>
    }
}

@code
{
    private SortedDictionary<int, List<DamageReport>> _damageReportsToShow = new();

    [Parameter]
    public bool OnlyNewest { get; set; }

    protected override void OnInitialized()
    {
        _userStateController.OnDamageReportsUpdated += OnDataChanged;
        _userStateController.OnPlayerOptionsUpdated += OnDataChanged;
    }

    protected override void OnParametersSet()
    {
        BuildDamageReportsToShow();
    }

    private void OnDataChanged()
    {
        BuildDamageReportsToShow();
        InvokeStateChange();
    }

    private void BuildDamageReportsToShow()
    {
        var showOtherPlayers = _userStateController.PlayerOptions?.ShowOtherPlayersDamageReports ?? false;
        var showMovement = _userStateController.PlayerOptions?.ShowMovementDamageReports ?? false;
        var result = new SortedDictionary<int, List<DamageReport>>();

        if (!_userStateController.DamageReportContainer.IsEmpty())
        {
            if (OnlyNewest)
            {
                var last = _userStateController.DamageReportContainer.DamageReports.Last();
                result.Add(last.Key, new());
                foreach (var damageReport in last.Value)
                {
                    if ((_userStateController.PlayerState?.IsSpectator == true || showOtherPlayers || _userStateController.DamageReportConcernsPlayer(damageReport)) &&
                        (showMovement || damageReport.Phase != Phase.Movement))
                    {
                        result[last.Key].Add(damageReport);
                    }
                }
            }
            else
            {
                foreach (var singleTurnDamageReports in _userStateController.DamageReportContainer.DamageReports.Reverse())
                {
                    var turn = singleTurnDamageReports.Key;
                    result.Add(turn, new());
                    foreach (var damageReport in singleTurnDamageReports.Value)
                    {
                        if ((_userStateController.PlayerState?.IsSpectator == true || showOtherPlayers || _userStateController.DamageReportConcernsPlayer(damageReport)) &&
                            (showMovement || damageReport.Phase != Phase.Movement))
                        {
                            result[turn].Add(damageReport);
                        }
                    }
                }
            }
        }

        _damageReportsToShow = result;
    }

    public void Dispose()
    {
        _userStateController.OnDamageReportsUpdated -= OnDataChanged;
        _userStateController.OnPlayerOptionsUpdated -= OnDataChanged;
    }
}
